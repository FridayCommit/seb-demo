apiVersion: v1
kind: Secret
metadata:
  name: github-creds
stringData:
  # The proper value for the GH_TOKEN can be apply locally, cba setting up something proper for this demo
  credentials: |
    { 
      "token": "${GH_TOKEN}"
    }
